const COOKIE = {
  COOKIE_NAME: 'accessToken',
};

const AXIOS_PATH = {
  USER: '/user',
  LOGIN: '/login',
  REGISTER: '/register',
  DIRAY: '/diary',
};

const QUERY = {
  AXIOS_PATH,
  COOKIE,
};
export default QUERY;
